# Windows-Registry-Change-Monitor
This project focuses on designing a Windows Registry Monitoring System that tracks unauthorized or suspicious changes made to registry keys.  The Windows Registry stores critical configuration data, startup locations, system settings, and application behavior. 
